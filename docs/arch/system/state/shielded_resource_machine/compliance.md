# Compliance

Each compliance unit contains one consumed and one created resource. The resources in the compliance unit are bound to each other: the nonce of the created resource is the nullifier of the consumed resource.

We compute delta per compliance unit.
